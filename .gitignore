# Node / Vite
node_modules/
dist/
dist-ssr/
*.local
.vite/

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.env
.env.*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store

# Azure / secrets — never commit credentials
*.pem
*.key
# NOTE: local.settings.json is deliberately tracked in this study repo — the checked-in copies
# hold placeholders only (e.g. "UseDevelopmentStorage=true"). Never paste a real connection
# string or key into one; use `func settings add` with the local emulator, or Key Vault.

# Clause local settings
.claude/
