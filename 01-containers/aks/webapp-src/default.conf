# Minimal nginx server block for the demo image.
# The only change from nginx's default is the port: 8080 instead of 80, so it
# matches webapp.yaml's containerPort, targetPort, and probe ports.
server {
    listen 8080;
    location / {
        root  /usr/share/nginx/html;
        index index.html;
    }
}
